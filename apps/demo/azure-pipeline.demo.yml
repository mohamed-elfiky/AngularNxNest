# Build numbering format
name: $(BuildID)

trigger:
  branches:
    include:
      - main
      - release/*
  paths:
    include:
      - 'apps/demo/*'

pool:
  vmImage: 'ubuntu-latest'

steps:
  - script: |
      echo "Hello from 'demo'"