# Build numbering format
name: $(BuildID)

trigger:
  branches:
    include:
      - master
      - release/*
  paths:
    exclude:
      - 'demo/*'
      - 'api/*'

pool:
  vmImage: 'ubuntu-latest'

steps:
  - script: |
      echo "Hello from '/' root folder."