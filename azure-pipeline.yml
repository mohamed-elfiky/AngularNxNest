# Build numbering format
name: $(BuildID)

trigger:
  branches:
    include:
      - main
      - release/*
  paths:
    exclude:
      - 'apps/demo/*'
      - 'apps/api/*'

pool:
  vmImage: 'ubuntu-latest'

steps:
  - script: |
      echo "Hello from '/' root folder."